import React from 'react';
import LoginForm from '../components/loginComponents/loginForm/LoginForm';

const LoginPage: React.FC = () => {
	return (
		<div className="login-body default-content">
			<LoginForm />
		</div>
	);
};

export default LoginPage;
